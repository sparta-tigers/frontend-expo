---
phase: 03
id: 03-01
status: draft
wave: 1
---

# Plan 03-01: Core Business Logic and Data Integrity Fixes

Refactor critical business logic paths to ensure data integrity, reliable persistence, and correct UI state.

## Tasks

### 1. Update src/features/exchange/api.ts (REQ-3.1)

- [ ] Locate coordinate and radius parameter checks.
- [ ] Replace `if (!lat)` style checks with `if (lat === undefined)` or nullish coalescing to support `0` values.

### 2. Update context/AuthContext.tsx (REQ-3.2)

- [ ] Wrap `AsyncStorage.setItem` in `updateMyTeam` with a try-catch block.
- [ ] Implement rollback logic or explicit error reporting if storage fails.

### 3. Update app/(tabs)/notification.tsx (REQ-3.3)

- [ ] Identify where `opponentTeam` is hardcoded.
- [ ] Replace with dynamic data from the notification/schedule object.

### 4. Update src/features/home/mocks.ts (REQ-3.4)

- [ ] Standardize team ID strings to match the canonical format used by `TEAM_NAME_TO_COLOR`.

### 5. Update src/utils/team.ts (REQ-3.5)

- [ ] Review `TEAM_NAME_TO_COLOR` and `TEAM_NAME_TO_ID` mappings.
- [ ] Add missing teams and ensure fallback colors are appropriate.

## Verification Plan

### Automated Tests

- [ ] Run `npm run check:all` to ensure no regressions.

### Manual Verification

1. **Coordinate Test**: Test location-based search with `lat: 0` or `lon: 0` (if applicable) or small radius.
2. **Team Storage**: Mock an `AsyncStorage` failure and verify the UI handles it gracefully.
3. **Notification UI**: Check the notification tab and verify `opponentTeam` displays the correct team name/logo instead of a placeholder.
4. **Mock Data**: Verify Home screen shows correct team colors and names in development mode.
