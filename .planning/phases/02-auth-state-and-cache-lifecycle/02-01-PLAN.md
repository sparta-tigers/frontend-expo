---
phase: 02
id: 02-01
status: draft
wave: 1
---

# Plan 02-01: Auth State Stability and Cache Isolation

Implement hydration-aware auth checks, user-isolated query keys, and explicit cache clearing on logout.

## Tasks

### 1. Update app/exchange/apply/[id].tsx (REQ-2.1)
- [ ] Add `isLoading` check to `useEffect` to prevent premature redirect.
- [ ] Ensure UI shows loading state while `isLoading` is true.

### 2. Update app/exchange/[id].tsx (REQ-2.2)
- [ ] Refine `isLoading` check to prevent authentication flicker.

### 3. Update app/(tabs)/_layout.tsx (REQ-2.3)
- [ ] Include `user?.userId` in the query key for `exchangeRequests`.

### 4. Update AuthContext.tsx & Logout Logic (REQ-2.4, REQ-2.5)
- [ ] Use `useQueryClient` in `AuthProvider` to call `queryClient.clear()` in `signout`.
- [ ] Make `MY_TEAM_STORAGE_KEY` dynamic based on `userId`.
- [ ] Update `loadToken` to load team info after identifying the user.
- [ ] Update `updateMyTeam` to use the dynamic key.

## Verification Plan

### Automated Tests
- N/A (Manual verification via UI behavior)

### Manual Verification
1. **Hydration Check**: Open a protected page (e.g., `/exchange/apply/1`) via deep link. Verify it doesn't immediately show "Please login" Alert before tokens are loaded.
2. **Account Switching**: Log in with User A, check "My Team". Log out, log in with User B. Verify User B's "My Team" is independent and User A's data (exchange requests) is gone from the UI/cache.
3. **Logout**: Verify that after logout, navigating back to a page doesn't show stale user data.
