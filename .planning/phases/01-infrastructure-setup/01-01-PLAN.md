---
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/core/persistence.ts
requirements_addressed:
  - REQ-1.1
  - REQ-1.2
autonomous: true
---

# Plan 01-01: Infrastructure Update

Update React Query to resolve peer dependency issues and fix misleading comments in the persistence layer.

## Tasks

<task id="1">
<title>Upgrade @tanstack/react-query</title>
<read_first>
- [package.json](file:///home/hyun2y00/01_Portfolio/01_Yaguniv/01_workspace/FEBE/frontend-expo/package.json)
</read_first>
<action>
Update the version of `@tanstack/react-query` from `^5.90.21` to `^5.100.9` in `package.json`.
Then run `npm install` to update the lockfile and node_modules.
</action>
<acceptance_criteria>
- `package.json` contains `"@tanstack/react-query": "^5.100.9"`
- `package-lock.json` reflects the updated version.
</acceptance_criteria>
</task>

<task id="2">
<title>Fix throttleTime comment in persistence.ts</title>
<read_first>
- [src/core/persistence.ts](file:///home/hyun2y00/01_Portfolio/01_Yaguniv/01_workspace/FEBE/frontend-expo/src/core/persistence.ts)
</read_first>
<action>
Replace the incorrect comment for `throttleTime` on line 14 with a clear explanation of its purpose.
Old: `/** 스토리지 제한에 걸릴 경우 오래된 데이터부터 삭제하는 로직 (기본값 활용) */`
New: `/** AsyncStorage에 캐시를 저장할 때 쓰기 빈도를 제한하는 옵션 (기본값 1000ms) */`
</action>
<acceptance_criteria>
- `src/core/persistence.ts` contains the updated comment.
- The comment accurately describes `throttleTime` as a write frequency limit.
</acceptance_criteria>
</task>

## Verification Plan

### Automated Tests
- Run `npm list @tanstack/react-query` to verify the installed version.
- Run `npm run check:all` (if available) to ensure no type errors or lint issues were introduced.

### Manual Verification
- Start the app using `npm run dev` and ensure it reaches the Metro bundler stage without errors.
