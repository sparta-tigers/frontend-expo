# AGENTS.md — expo-spartatigers

> Auto-generated by the dev pipeline scanner. Do not edit manually.
> Last updated: 2026-07-12T17:21:43+09:00
> ⚠️ To update this file, architectural changes must be detected by the scanner and confirmed by a human.

## How to Read This File

Every agent in this pipeline reads this file before doing any work.
It defines the rules, patterns, and guardrails specific to this project.

## Stack Context

React Native + Expo Router + Zustand + React Query + React Native Paper + STOMP WebSockets

## Code Style Rules

- DO use strict TypeScript typing and Zod schemas for runtime API validation.
- DO use the centralized `Logger` utility (`@/src/utils/logger`) instead of raw console logs.
- DON'T commit with ESLint warnings (`eslint --max-warnings=0` is enforced by Husky).

## Architecture Guardrails

- This project uses Feature-Sliced Design. Module boundaries are strictly enforced by `dependency-cruiser` (`scripts/architecture-guard`).
- Cross-feature entanglement is banned. Follow the unidirectional dependency graph.

## Testing Requirements

- Current unit test setup is minimal. Rely on static analysis and `dependency-cruiser` boundary tests (`npm run arch:all`, `npm run arch:dep`).
- Use `fast-check` for any property-based testing.

## Modularity Conventions

- Shared UI components → `/components/`
- Business logic & feature-specific code → `/src/features/[feature_name]/`
- Global configs & API clients → `/src/core/`
- Global client state → `/src/store/`
- Routing/Pages → `/app/`

## Security Rules (All Agents)

- Never hardcode secrets, tokens, or credentials.
- Use environment variables for all sensitive config.
- Persist tokens securely using `AsyncStorage`.
- Ensure API data passes through Zod boundary validation before use.

## Agent-Specific Instructions

### Orchestrator

Always verify if a change crosses feature boundaries (`src/features/`) and ensure `dependency-cruiser` rules are not violated.

### Architect

When adding new features, respect the `dependency-cruiser` configuration. All server state must use TanStack Query; all client state must use Zustand.

### Developer

Use `axios` interceptors with the Mutex queue for any token refresh logic. Maintain `react-error-boundary` for component-level crash handling.

### PR Reviewer

Any PR bypassing `dependency-cruiser` or adding circular dependencies is a 🔴 Critical violation. Ensure Zod schemas match API contracts.

### QA Agent

Focus on WebSocket connectivity (STOMP) stability, offline support via `react-query-persist-client`, and seamless 401 token refresh flows.
